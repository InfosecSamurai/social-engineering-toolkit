# Email Configuration
EMAIL_SERVER = smtp.example.com
EMAIL_PORT = 587
EMAIL_USERNAME = ${EMAIL_USERNAME}  # Use environment variable for sensitive data
EMAIL_PASSWORD = ${EMAIL_PASSWORD}  # Use environment variable for sensitive data

# Twilio Configuration
TWILIO_SID = ${TWILIO_SID}  # Use environment variable for sensitive data
TWILIO_AUTH = ${TWILIO_AUTH}  # Use environment variable for sensitive data
TWILIO_PHONE = +1234567890

# Fake Wi-Fi Configuration
FAKE_WIFI_SSID = Free_WiFi
FAKE_WIFI_CHANNEL = 6
FAKE_WIFI_INTERFACE = wlan0
